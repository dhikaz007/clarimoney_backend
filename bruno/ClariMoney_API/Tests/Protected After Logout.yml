info:
  name: Protected Request After Logout
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/api/v1/categories"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Logged-out access is rejected", () => {
          expect(res.status).to.equal(401);
        });
        
examples:
  - name: Unauthorized
    status: 401
    body: |
      {"status_code": 401, "message": "Missing or invalid token"}
