info:
  name: Protected Request After Logout All
  type: http
  seq: 15

http:
  method: GET
  url: "{{base_url}}/api/v1/categories"
  auth:
    type: bearer
    token: "{{access_token}}"

tests: |
  test("All-device logout rejects access", () => {
    expect(res.status).to.equal(401);
  });

examples:
  - name: Unauthorized
    status: 401
    body: |
      {"status_code": 401, "message": "Missing or invalid token"}
