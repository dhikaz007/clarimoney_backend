info:
  name: Protected Request After Device Revoke
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/v1/categories"
  auth:
    type: bearer
    token: "{{revoked_access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Revoked device access is rejected", () => {
          expect(res.status).to.equal(401);
        });
        
examples:
  - name: Unauthorized
    status: 401
    body: |
      {"status_code": 401, "message": "Missing or invalid token"}
