info:
  name: Second Device Before Logout All
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/api/v1/categories"
  auth:
    type: bearer
    token: "{{access_token_2}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Second-device access works before logout-all", () => {
          expect(res.status).to.equal(200);
        });
        test("Second-device token remains isolated", () => {
          expect(bru.getEnvVar("access_token_2")).to.be.a("string").and.not.empty;
          expect(bru.getEnvVar("access_token_2")).to.not.equal(bru.getEnvVar("access_token"));
        });
        
examples:
  - name: Success
    status: 200
    body: |
      {"status_code": 200, "message": "Categories fetched successfully", "data": []}
