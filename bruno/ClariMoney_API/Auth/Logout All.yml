info:
  name: Logout All Devices
  type: http
  seq: 14

http:
  method: POST
  url: "{{base_url}}/api/v1/auth/logout-all"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", () => {
          expect(res.status).to.equal(200);
        });
        
examples:
  - name: Success
    status: 200
    body: |
      {"status_code": 200, "message": "Logged out from all devices", "data": null}
