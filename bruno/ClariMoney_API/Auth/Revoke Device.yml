info:
  name: Revoke Device
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/api/v1/auth/sessions/{{session_id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

tests: |
  test("Status is 200", () => {
    expect(res.status).to.equal(200);
  });
