info:
  name: Revoke Device
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/api/v1/auth/sessions/{{session_id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: before-request
      code: |
        bru.setEnvVar("revoked_access_token", bru.getEnvVar("access_token"));

tests: |
  test("Status is 200", () => {
    expect(res.status).to.equal(200);
  });

examples:
  - name: Success
    status: 200
    body: |
      {"status_code": 200, "message": "Session revoked", "data": null}
