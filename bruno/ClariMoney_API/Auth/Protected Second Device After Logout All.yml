info:
  name: Second Device After Logout All
  type: http
  seq: 16

http:
  method: GET
  url: "{{base_url}}/api/v1/categories"
  auth:
    type: bearer
    token: "{{access_token_2}}"

tests: |
  test("Logout-all revokes second device", () => {
    expect(res.status).to.equal(401);
  });

examples:
  - name: Unauthorized
    status: 401
    body: |
      {"status_code": 401, "message": "Missing or invalid token"}
